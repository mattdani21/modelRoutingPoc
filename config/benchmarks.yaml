# Generated by scripts/generate_benchmarks.mjs. Do not edit by hand.
# Synthetic data only. Each task is one reviewed test case within a task class.
version: "2026-07-23.1"
tasks:
  - id: ACT-EAC2-001
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 15 years.
      The extension term is 5 years.
      The disclosed total term is 15 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: high
        finding_code: DISCLOSED_TERM_MISMATCH
        required_action_code: CORRECT_TOTAL_TERM_BEFORE_ISSUE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-002
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 20 years.
      The extension term is 7 years.
      The disclosed total term is 20 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: high
        finding_code: DISCLOSED_TERM_MISMATCH
        required_action_code: CORRECT_TOTAL_TERM_BEFORE_ISSUE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-003
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 25 years.
      The extension term is 9 years.
      The disclosed total term is 25 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: high
        finding_code: DISCLOSED_TERM_MISMATCH
        required_action_code: CORRECT_TOTAL_TERM_BEFORE_ISSUE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-004
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 30 years.
      The extension term is 3 years.
      The disclosed total term is 33 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: low
        finding_code: TERM_MATCH
        required_action_code: NONE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-005
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 35 years.
      The extension term is 5 years.
      The disclosed total term is 35 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: high
        finding_code: DISCLOSED_TERM_MISMATCH
        required_action_code: CORRECT_TOTAL_TERM_BEFORE_ISSUE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-006
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 10 years.
      The extension term is 7 years.
      The disclosed total term is 10 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: high
        finding_code: DISCLOSED_TERM_MISMATCH
        required_action_code: CORRECT_TOTAL_TERM_BEFORE_ISSUE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-007
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 15 years.
      The extension term is 9 years.
      The disclosed total term is 15 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: high
        finding_code: DISCLOSED_TERM_MISMATCH
        required_action_code: CORRECT_TOTAL_TERM_BEFORE_ISSUE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-008
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 20 years.
      The extension term is 3 years.
      The disclosed total term is 23 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: low
        finding_code: TERM_MATCH
        required_action_code: NONE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-009
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 25 years.
      The extension term is 5 years.
      The disclosed total term is 25 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: high
        finding_code: DISCLOSED_TERM_MISMATCH
        required_action_code: CORRECT_TOTAL_TERM_BEFORE_ISSUE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-010
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 30 years.
      The extension term is 7 years.
      The disclosed total term is 30 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: high
        finding_code: DISCLOSED_TERM_MISMATCH
        required_action_code: CORRECT_TOTAL_TERM_BEFORE_ISSUE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-011
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 35 years.
      The extension term is 9 years.
      The disclosed total term is 35 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: high
        finding_code: DISCLOSED_TERM_MISMATCH
        required_action_code: CORRECT_TOTAL_TERM_BEFORE_ISSUE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-012
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 10 years.
      The extension term is 3 years.
      The disclosed total term is 13 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: low
        finding_code: TERM_MATCH
        required_action_code: NONE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-013
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 15 years.
      The extension term is 5 years.
      The disclosed total term is 15 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: high
        finding_code: DISCLOSED_TERM_MISMATCH
        required_action_code: CORRECT_TOTAL_TERM_BEFORE_ISSUE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-014
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 20 years.
      The extension term is 7 years.
      The disclosed total term is 20 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: high
        finding_code: DISCLOSED_TERM_MISMATCH
        required_action_code: CORRECT_TOTAL_TERM_BEFORE_ISSUE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: ACT-EAC2-015
    prompt_version: "1.1"
    department: "Actuarial"
    team: "Product Calculations"
    business_value: "Prevent an incorrect EAC2 disclosure and reduce manual review time."
    process: "Review an EAC2 extension scenario."
    task_class: controlled_reasoning
    data_classification: internal
    prompt: |
      Review this synthetic EAC2 case.
      The base term is 25 years.
      The extension term is 9 years.
      The disclosed total term is 25 years.
      Return this JSON shape with no extra fields:
      {"risk":"high|low","finding_code":"DISCLOSED_TERM_MISMATCH|TERM_MATCH","required_action_code":"CORRECT_TOTAL_TERM_BEFORE_ISSUE|NONE"}
    grader:
      type: json_equals
      expected:
        risk: high
        finding_code: DISCLOSED_TERM_MISMATCH
        required_action_code: CORRECT_TOTAL_TERM_BEFORE_ISSUE
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: SYS-CTRL-001
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 103.63.
      MTS value: 101.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 2.6
        status: outside_tolerance
        investigation_priority: high
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-002
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 103.12.
      MTS value: 102.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 1.1
        status: within_tolerance
        investigation_priority: normal
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-003
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 105.27.
      MTS value: 103.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 2.2
        status: outside_tolerance
        investigation_priority: high
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-004
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 104.83.
      MTS value: 104.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 0.8
        status: within_tolerance
        investigation_priority: normal
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-005
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 108.15.
      MTS value: 105.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 3
        status: outside_tolerance
        investigation_priority: high
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-006
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 106.53.
      MTS value: 106.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 0.5
        status: within_tolerance
        investigation_priority: normal
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-007
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 109.78.
      MTS value: 107.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 2.6
        status: outside_tolerance
        investigation_priority: high
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-008
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 109.19.
      MTS value: 108.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 1.1
        status: within_tolerance
        investigation_priority: normal
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-009
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 111.4.
      MTS value: 109.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 2.2
        status: outside_tolerance
        investigation_priority: high
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-010
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 110.88.
      MTS value: 110.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 0.8
        status: within_tolerance
        investigation_priority: normal
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-011
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 114.33.
      MTS value: 111.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 3
        status: outside_tolerance
        investigation_priority: high
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-012
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 112.56.
      MTS value: 112.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 0.5
        status: within_tolerance
        investigation_priority: normal
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-013
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 115.94.
      MTS value: 113.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 2.6
        status: outside_tolerance
        investigation_priority: high
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-014
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 115.25.
      MTS value: 114.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 1.1
        status: within_tolerance
        investigation_priority: normal
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: SYS-CTRL-015
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Systems Control"
    business_value: "Find calculation differences before a release reaches production."
    process: "Triage an ACE-to-MTS control difference."
    task_class: structured_triage
    data_classification: confidential
    prompt: |
      Review this synthetic control result.
      ACE value: 117.53.
      MTS value: 115.
      The allowed relative difference is 2.00 percent.
      Calculate the absolute relative difference against MTS.
      Return this JSON shape with no extra fields:
      {"relative_difference_percent":0.0,"status":"within_tolerance|outside_tolerance","investigation_priority":"normal|high"}
    grader:
      type: json_equals
      expected:
        relative_difference_percent: 2.2
        status: outside_tolerance
        investigation_priority: high
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 5000

  - id: QE-TEST-001
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A premium waiver applies only while a disability claim is active.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-002
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A paid-up value must never exceed the sum assured.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-003
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A loan balance plus interest must not exceed the surrender value.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-004
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A maturity payout must include all vested bonuses.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-005
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A policy can have a base term and one extension term. The EAC2 letter must show the total term.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-006
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A premium waiver applies only while a disability claim is active.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-007
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A paid-up value must never exceed the sum assured.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-008
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A loan balance plus interest must not exceed the surrender value.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-009
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A maturity payout must include all vested bonuses.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-010
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A policy can have a base term and one extension term. The EAC2 letter must show the total term.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-011
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A premium waiver applies only while a disability claim is active.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-012
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A paid-up value must never exceed the sum assured.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-013
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A loan balance plus interest must not exceed the surrender value.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-014
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A maturity payout must include all vested bonuses.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: QE-TEST-015
    prompt_version: "1.0"
    department: "Actuarial Systems"
    team: "Test Analysis"
    business_value: "Reduce test design time and improve control coverage."
    process: "Draft tests for an EAC2 rule change."
    task_class: test_design
    data_classification: internal
    prompt: |
      Draft a compact test plan for this synthetic rule.
      A policy can have a base term and one extension term. The EAC2 letter must show the total term.
      Return one JSON object.
      Use these fields: happy_path, boundary_case, negative_case, expected_control.
    grader:
      type: json_fields
      fields: [happy_path, boundary_case, negative_case, expected_control]
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 8000

  - id: LOG-GREP-001
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL101.
      Keep their original order.

      INFO request=1 policy=POL101 status=started
      ERROR request=1 policy=POL101 code=E401 source=MTS
      ERROR request=2 policy=POL901 code=E500 source=MTS
      WARN request=1 policy=POL101 retry=true
      ERROR request=1 policy=POL101 code=E501 source=ROUTER
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL101 code=E401 source=MTS"
        - "ERROR request=1 policy=POL101 code=E501 source=ROUTER"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-002
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL102.
      Keep their original order.

      INFO request=1 policy=POL102 status=started
      ERROR request=1 policy=POL102 code=E402 source=ROUTER
      ERROR request=2 policy=POL902 code=E500 source=MTS
      WARN request=1 policy=POL102 retry=true
      ERROR request=1 policy=POL102 code=E502 source=GATEWAY
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL102 code=E402 source=ROUTER"
        - "ERROR request=1 policy=POL102 code=E502 source=GATEWAY"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-003
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL103.
      Keep their original order.

      INFO request=1 policy=POL103 status=started
      ERROR request=1 policy=POL103 code=E403 source=GATEWAY
      ERROR request=2 policy=POL903 code=E500 source=MTS
      WARN request=1 policy=POL103 retry=true
      ERROR request=1 policy=POL103 code=E503 source=ACE
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL103 code=E403 source=GATEWAY"
        - "ERROR request=1 policy=POL103 code=E503 source=ACE"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-004
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL104.
      Keep their original order.

      INFO request=1 policy=POL104 status=started
      ERROR request=1 policy=POL104 code=E404 source=ACE
      ERROR request=2 policy=POL904 code=E500 source=MTS
      WARN request=1 policy=POL104 retry=true
      ERROR request=1 policy=POL104 code=E504 source=MTS
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL104 code=E404 source=ACE"
        - "ERROR request=1 policy=POL104 code=E504 source=MTS"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-005
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL105.
      Keep their original order.

      INFO request=1 policy=POL105 status=started
      ERROR request=1 policy=POL105 code=E405 source=MTS
      ERROR request=2 policy=POL905 code=E500 source=MTS
      WARN request=1 policy=POL105 retry=true
      ERROR request=1 policy=POL105 code=E505 source=ROUTER
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL105 code=E405 source=MTS"
        - "ERROR request=1 policy=POL105 code=E505 source=ROUTER"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-006
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL106.
      Keep their original order.

      INFO request=1 policy=POL106 status=started
      ERROR request=1 policy=POL106 code=E406 source=ROUTER
      ERROR request=2 policy=POL906 code=E500 source=MTS
      WARN request=1 policy=POL106 retry=true
      ERROR request=1 policy=POL106 code=E506 source=GATEWAY
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL106 code=E406 source=ROUTER"
        - "ERROR request=1 policy=POL106 code=E506 source=GATEWAY"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-007
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL107.
      Keep their original order.

      INFO request=1 policy=POL107 status=started
      ERROR request=1 policy=POL107 code=E407 source=GATEWAY
      ERROR request=2 policy=POL907 code=E500 source=MTS
      WARN request=1 policy=POL107 retry=true
      ERROR request=1 policy=POL107 code=E507 source=ACE
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL107 code=E407 source=GATEWAY"
        - "ERROR request=1 policy=POL107 code=E507 source=ACE"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-008
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL108.
      Keep their original order.

      INFO request=1 policy=POL108 status=started
      ERROR request=1 policy=POL108 code=E408 source=ACE
      ERROR request=2 policy=POL908 code=E500 source=MTS
      WARN request=1 policy=POL108 retry=true
      ERROR request=1 policy=POL108 code=E508 source=MTS
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL108 code=E408 source=ACE"
        - "ERROR request=1 policy=POL108 code=E508 source=MTS"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-009
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL109.
      Keep their original order.

      INFO request=1 policy=POL109 status=started
      ERROR request=1 policy=POL109 code=E409 source=MTS
      ERROR request=2 policy=POL909 code=E500 source=MTS
      WARN request=1 policy=POL109 retry=true
      ERROR request=1 policy=POL109 code=E509 source=ROUTER
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL109 code=E409 source=MTS"
        - "ERROR request=1 policy=POL109 code=E509 source=ROUTER"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-010
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL110.
      Keep their original order.

      INFO request=1 policy=POL110 status=started
      ERROR request=1 policy=POL110 code=E410 source=ROUTER
      ERROR request=2 policy=POL910 code=E500 source=MTS
      WARN request=1 policy=POL110 retry=true
      ERROR request=1 policy=POL110 code=E510 source=GATEWAY
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL110 code=E410 source=ROUTER"
        - "ERROR request=1 policy=POL110 code=E510 source=GATEWAY"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-011
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL111.
      Keep their original order.

      INFO request=1 policy=POL111 status=started
      ERROR request=1 policy=POL111 code=E411 source=GATEWAY
      ERROR request=2 policy=POL911 code=E500 source=MTS
      WARN request=1 policy=POL111 retry=true
      ERROR request=1 policy=POL111 code=E511 source=ACE
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL111 code=E411 source=GATEWAY"
        - "ERROR request=1 policy=POL111 code=E511 source=ACE"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-012
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL112.
      Keep their original order.

      INFO request=1 policy=POL112 status=started
      ERROR request=1 policy=POL112 code=E412 source=ACE
      ERROR request=2 policy=POL912 code=E500 source=MTS
      WARN request=1 policy=POL112 retry=true
      ERROR request=1 policy=POL112 code=E512 source=MTS
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL112 code=E412 source=ACE"
        - "ERROR request=1 policy=POL112 code=E512 source=MTS"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-013
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL113.
      Keep their original order.

      INFO request=1 policy=POL113 status=started
      ERROR request=1 policy=POL113 code=E413 source=MTS
      ERROR request=2 policy=POL913 code=E500 source=MTS
      WARN request=1 policy=POL113 retry=true
      ERROR request=1 policy=POL113 code=E513 source=ROUTER
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL113 code=E413 source=MTS"
        - "ERROR request=1 policy=POL113 code=E513 source=ROUTER"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-014
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL114.
      Keep their original order.

      INFO request=1 policy=POL114 status=started
      ERROR request=1 policy=POL114 code=E414 source=ROUTER
      ERROR request=2 policy=POL914 code=E500 source=MTS
      WARN request=1 policy=POL114 retry=true
      ERROR request=1 policy=POL114 code=E514 source=GATEWAY
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL114 code=E414 source=ROUTER"
        - "ERROR request=1 policy=POL114 code=E514 source=GATEWAY"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

  - id: LOG-GREP-015
    prompt_version: "1.1"
    department: "Actuarial Systems"
    team: "Production Support"
    business_value: "Reduce log investigation time from minutes to seconds."
    process: "Find the relevant policy errors in a service log."
    task_class: log_extraction
    data_classification: confidential
    prompt: |
      Read the synthetic log lines.
      Return only the two ERROR lines that contain policy POL115.
      Keep their original order.

      INFO request=1 policy=POL115 status=started
      ERROR request=1 policy=POL115 code=E415 source=GATEWAY
      ERROR request=2 policy=POL915 code=E500 source=MTS
      WARN request=1 policy=POL115 retry=true
      ERROR request=1 policy=POL115 code=E515 source=ACE
    grader:
      type: exact_lines
      ignore_order: false
      expected:
        - "ERROR request=1 policy=POL115 code=E415 source=GATEWAY"
        - "ERROR request=1 policy=POL115 code=E515 source=ACE"
    quality_gate:
      deterministic_pass_required: true
      minimum_human_score: 4
      maximum_latency_ms: 3000

